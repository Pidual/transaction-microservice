server:
  port: 8092
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/transactions
    username: admin
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true